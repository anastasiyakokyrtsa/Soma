# Бэклог — решено, но не сделано

Не append-only (в отличие от `decisions.md`) — рабочий чек-лист, вычёркиваем
по мере реализации. Полное обоснование каждого пункта — в `decisions.md`
или `findings.md`, здесь только напоминание и привязка к экрану/фиче.

**Правило:** перед началом работы над экраном/фичей — сверяться с этим
файлом, нет ли для него отложенных пунктов.

## Онбординг

- [ ] **Восстановление брошенного онбординга.** При повторном открытии,
  если онбординг не завершён — попадать на тот шаг, где остановились, не
  на Start заново. Базовое поведение, не опция.
- [ ] **Ненавязчивая плашка-напоминание на Home** (стиль как у
  `MoodCheckIn`) для тех, кто не закончил онбординг — «дополни ещё пару
  вопросов о себе», показывать редко (раз в несколько открытий), можно
  закрыть, без укоряющего тона. См. `decisions.md` 2026-09-15.
- [ ] **Переставить шаг «Дата рождения» раньше** — сразу после «Выбор
  подхода», а не ближе к концу потока. Единственный ввод, нужный для
  биоритмов.
- [ ] **Оживить экран «Персонализация» (фаза 1)** — вместо пустого
  спиннера показать одну фразу-инсайт про сегодняшний день + маленький
  значок-акцент (не полный график). Полный график с тремя кривыми —
  на фазе 2, ближе к концу, как более крупный reveal.
- [ ] Валидация даты рождения на диапазон (находка №17) + дублирование
  даты рождения рядом с биоритмами на Home для самопроверки.
- [ ] Нативный date picker вместо кастомного колеса, отдельно для iOS и
  Android (находка №13).

## Профиль (экран не построен)

- [ ] Базовый график сна, даты цикла, «в чём нужна поддержка» — как
  настройки-анкета, единственный источник правды для этих полей.

## «+» (кнопка в нижнем баре, роль не финализирована)

- [ ] Предложение: 3 типа быстрой записи — Сон, Цикл (только релевантным
  профилям), «Самочувствие сейчас» (точечный симптом/состояние,
  несколько раз в день, не то же самое что ежедневное настроение).
  Открытие — мини-меню из тайлов, не единая форма. **Ждёт её решения,
  она думает сама.**
- [ ] Интеграция с Apple Health/Google Fit/Garmin для сна — параллельно
  с ручным вводом, не вместо него.

## Аналитика

- [ ] Добавить 7-ю карточку **Цикл** (отдельно от Луны).
- [ ] Луна/Солнце — формулировки на уровне честности чая («некоторые
  исследования связывают»), не как доказанный факт.
- [ ] Паттерны — отложено на отдельное обсуждение. Принцип на будущее:
  не показывать связь как связь до ~14 согласованных пар наблюдений;
  биоритм↔самочувствие никогда не подавать как связь.
- [ ] Карточка «Состояния» — история настроения + точечная правка любого
  прошлого дня (закрывает находку №6, тем же способом что Сон/Цикл).

## Дневник (экран не построен)

- [ ] **Нерешённое противоречие**: не ясно, правится ли запись из «+»
  в Дневнике (сырая лента) или только в Аналитике (по метрике). Нужно
  явное решение перед тем, как строить любой из этих двух экранов.

## Wellbeing / безопасность

- [ ] При N дней подряд низкого ресурса — мягкое напоминание о
  профессиональной поддержке (находка №4). Актуально, когда появятся
  сценарии для нескольких состояний, не только «низкий ресурс».

## Технический долг (не блокирует, но дёшево сейчас/дорого потом)

- [ ] Шифрование чувствительных полей (`expo-secure-store`) при появлении
  реальной персистентности (находка №14).
- [ ] Проверить приветствие на Home/заголовок Care с VoiceOver/TalkBack —
  возможно не озвучивается из-за SvgText (находка №16).
