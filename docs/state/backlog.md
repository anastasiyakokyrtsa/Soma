# Бэклог — решено, но не сделано

Не append-only (в отличие от `decisions.md`) — рабочий чек-лист, вычёркиваем
по мере реализации. Полное обоснование каждого пункта — в `decisions.md`
или `findings.md`, здесь только напоминание и привязка к экрану/фиче.

**Правило:** перед началом работы над экраном/фичей — сверяться с этим
файлом, нет ли для него отложенных пунктов.

## Онбординг

- [ ] **Проверить размер картинки на `ProfileStartScreen` на реальном
  устройстве.** Сейчас (2026-09-20) — намеренный нахлёст за края экрана
  (`illustrationWidth = width + 64`), подобранный не точным расчётом, а
  на глаз по сжатому скриншоту. Может понадобиться ещё увеличить или
  уменьшить — см. `decisions.md` 2026-09-20 за полную историю (там же
  урок про то, что низкий порог альфа-канала у файлов с мягкими краями
  завышает видимый отступ).
- [ ] **Более сложная анимация для картинок «О приложении» 1-3**
  (Rive/Lottie, отдельные движущиеся элементы — мерцание звёзд,
  вращение орбит), её явный запрос. Требует перерисовки иллюстраций как
  слоёв, не только кода. Рекомендация — Rive (визуальный редактор,
  `.riv`, `rive-react-native`), не Lottie. Не начато.
- [ ] **Заменить `EyeIcon.tsx` на настоящий актив из Figma** (node
  806-2157) — сейчас это обычный глаз-глиф, не 1:1 копия, потому что
  Figma-коннектор недоступен в этой среде. Заменить, когда появится
  доступ или она сама пришлёт экспорт.
- [ ] **`RadioChoiceCard` — выравнивание текста по верху вместо центра.**
  Команда (ui-designer) рекомендует top-align для карточек с разной
  длиной описания (стандартный паттерн для списков опций), но владелец
  сознательно оставила центрирование «пока» (2026-09-20). Не менять без
  её просьбы — это не забытая рекомендация, а её осознанный выбор.
- [ ] **`ExpandableChoiceCard.tsx` не используется нигде, кроме
  комментариев** (заменён на `RadioChoiceCard` 2026-09-20) — не удалён,
  ждёт её решения, стоит ли убирать.
- [ ] **Вернуть находку №13 к настоящему per-platform пикеру.** Сейчас
  `ProfileDateOfBirthScreen.tsx` показывает старый кастомный
  iOS-стилизованный `DateWheelPicker` на обеих платформах намеренно
  (`FORCE_IOS_WHEEL_PREVIEW = true`, коммит `fa170b3`, 2026-09-19) — её
  явный запрос, пока фокус на iPhone-пользователях. Настоящий нативный
  `@react-native-community/datetimepicker` (iOS spinner / Android
  диалог) никуда не делся, просто выключен этим флагом. **Когда фокус
  расширится за пределы iPhone-превью — поставить флаг в `false`.**
- [ ] **Восстановление брошенного онбординга.** При повторном открытии,
  если онбординг не завершён — попадать на тот шаг, где остановились, не
  на Start заново. Базовое поведение, не опция.
- [ ] **Ненавязчивая плашка-напоминание на Home** (стиль как у
  `MoodCheckIn`) для тех, кто не закончил онбординг — «дополни ещё пару
  вопросов о себе», показывать редко (раз в несколько открытий), можно
  закрыть, без укоряющего тона. См. `decisions.md` 2026-09-15.
- [x] ~~Переставить шаг «Дата рождения» раньше~~ — владелец решила оставить
  дату рождения на текущем месте (2026-09-15), не переставлять.
- [x] **Механика раннего инсайта после даты рождения — сделана** (коммит
  `138009c`, 2026-09-15): новый экран `BiorhythmInsightScreen` сразу
  после «Когда ты родился?», настоящий расчёт по классической формуле
  (`lib/biorhythm.ts`), одна фраза про самый выраженный сегодня цикл, не
  полный график. Существующие экраны «Персонализация» не трогали —
  сделан отдельный новый экран, не переиспользование старых.
- [ ] **Визуал этого экрана требует доработки** — механика одобрена,
  оформление нет (владелец, 2026-09-15). Что именно не устраивает —
  уточнить в следующий раз.
- [x] ~~Нативный date picker вместо кастомного колеса~~ — сделано
  (находка №13, коммит `0b52f73`, 2026-09-18): `@react-native-community/
  datetimepicker`, iOS — spinner инлайн, Android — модальный диалог по
  тапу на поле. `DateWheelPicker.tsx` удалён, был не нужен нигде больше.
- [x] ~~Диапазон дат (не будущее, не старше 120 лет)~~ — частично сделано
  тем же коммитом, через `minimumDate`/`maximumDate` пикера.
- [x] ~~Показать дату рождения рядом с биоритмами для самопроверки~~ —
  сделано (находка №17 закрыта полностью, коммит `e5151a7`,
  2026-09-18). Не на Home — только внутри шторки (i) на экране Биоритмов
  (ux-architect + ui-designer сошлись на этом месте). Дата пока
  захардкожена (как и весь остальной контент этого экрана) — станет
  настоящей вместе с реальным состоянием/персистентностью.

## Статьи

- [x] **«Вода как основа баланса» — построена** (коммит `ef60d0e`,
  2026-09-16). Была сломанная ссылка (`ArticleWater` вызывался, экрана не
  было) — оказалось, блокер был основан на неверном чтении «01/07» как
  «нужно ещё 4-7 страниц текста»; на деле это номер статьи в серии
  «Основы восстановления», а не внутренняя пагинация. Она прислала
  полный готовый текст, сама выбрала формат — свайп-страницы (её
  референс-дизайн), не непрерывный скролл, который советовала команда.
  9 страниц, обосновано и согласовано с ней напрямую.
- [ ] Ещё 2 статьи серии «Основы восстановления» уже написаны черновиком
  (`docs/content/статьи.md` — «Свет и внутренние часы», «Паузы и
  восстановление»), но не построены. `ArticleWaterScreen`/
  `content/articleWater.ts` сделаны достаточно обобщённо (общая модель
  `ArticleBlock`), чтобы для них не пришлось строить экран с нуля —
  только разбить текст на страницы и завести свой content-файл.

## Профиль (экран не построен)

- [ ] Базовый график сна, даты цикла, «в чём нужна поддержка» — как
  настройки-анкета, единственный источник правды для этих полей.

## «+» (кнопка в нижнем баре, роль не финализирована)

- [ ] Предложение: 3 типа быстрой записи — Сон, Цикл (только релевантным
  профилям), «Самочувствие сейчас» (точечный симптом/состояние,
  несколько раз в день, не то же самое что ежедневное настроение).
  Открытие — мини-меню из тайлов, не единая форма. **Ждёт её решения,
  она думает сама.**
- [ ] Интеграция с Apple Health/Google Fit/Garmin для сна — параллельно
  с ручным вводом, не вместо него.

## Аналитика

- [ ] Добавить 7-ю карточку **Цикл** (отдельно от Луны).
- [ ] Луна/Солнце — формулировки на уровне честности чая («некоторые
  исследования связывают»), не как доказанный факт.
- [ ] Паттерны — отложено на отдельное обсуждение. Принцип на будущее:
  не показывать связь как связь до ~14 согласованных пар наблюдений;
  биоритм↔самочувствие никогда не подавать как связь.
- [ ] Карточка «Состояния» — история настроения + точечная правка любого
  прошлого дня, включая быструю догонку «а как было вчера?» на Home
  (закрывает находку №6, тем же способом что Сон/Цикл). **Единственное**
  место правки истории — см. `decisions.md` 2026-09-16, Дневник этой
  роли не имеет.

## Дневник (экран не построен)

- [ ] Роль ещё не определена. **Точно НЕ** место правки структурированных
  данных (сон/цикл/настроение/«+») — это закреплено за Аналитикой,
  противоречие из-за дрейфа между раундами консультации закрыто
  2026-09-16.

## Wellbeing / безопасность

- [ ] При N дней подряд низкого ресурса — мягкое напоминание о
  профессиональной поддержке (находка №4). Актуально, когда появятся
  сценарии для нескольких состояний, не только «низкий ресурс».

## Технический долг (не блокирует, но дёшево сейчас/дорого потом)

- [ ] Шифрование чувствительных полей (`expo-secure-store`, не
  `AsyncStorage`) — **решение зафиксировано** в `decisions.md`
  2026-09-18, находка №14 закрыта; сам код появится только вместе с
  реальной персистентностью, когда бы она ни началась строиться.
- [ ] **Проверить на реальном устройстве** с включённым VoiceOver/TalkBack,
  что приветствие на Home и заголовок Care теперь озвучиваются —
  `accessibilityLabel` добавлен (коммит `dd6a091`, находка №16), но
  фактическое звучание руками не проверено.
